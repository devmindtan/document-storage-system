# Copy sang .env và điền giá trị thật khi triển khai (không commit .env).
# Lưu ý: code hiện chỉ đọc os.environ trực tiếp (core/config.py), không tự
# nạp file .env — cần `export $(cat .env | xargs)` hoặc set biến môi trường
# theo cách khác trước khi chạy uvicorn/make run.
SESSION_SECRET_KEY=
